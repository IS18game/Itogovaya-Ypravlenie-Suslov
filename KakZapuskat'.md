Скрипт обходит веб-сайты, начиная с заданного URL, и извлекает контактную информацию:

- 📧 Email-адреса
- 📞 Телефоны (только российские,в формате +7XXXXXXXXXX)
- 📍 Почтовые адреса (в формате: город, улица, дом)

Контакты сохраняются в базу данных (H2, in-memory) без дубликатов. Предусмотрен REST-интерфейс для запуска краулера, просмотра и очистки результатов.

Как запустить

CrawlerApplication ПКМ RUN(либо справа вверху нажать RUN)

После запуска приложения, H2-консоль доступна по адресу:
http://localhost:8080/h2-console

Параметры подключения:

| Поле              | Значение                          |
|-------------------|-----------------------------------|
| JDBC URL      | jdbc:h2:mem:crawlerdb           |
| Username      | sa                              |
| Password      | (оставить пустым)               |

Проверка данных
1. Перейти на http://localhost:8080/h2-console
2. Введите параметры подключения (таблица выше)
3. Нажмите Connect
4. Выполните SQL-запрос:
SELECT * FROM contact_info;

| Метод | URL | Описание |
|-------|-----|----------|
| POST | /start?url=... | Запуск краулера с указанного URL |
| GET | /contacts | Получить все найденные контакты |
| DELETE | /contacts | Очистить базу контактов |
| GET | /status | Получить статистику по найденным контактам |

Пример:
PowerShell
Invoke-WebRequest -Uri "http://localhost:8080/api/start?url=https://rumyanskiy.ru" -Method POST   (вместо https://rumyanskiy.ru можете вставить свою ссылку сайта)

📥 Получить все контакты
PowerShell
Invoke-WebRequest -Uri "http://localhost:8080/api/contacts"

📊 Получить статистику
PowerShell
Invoke-WebRequest -Uri "http://localhost:8080/api/status"

🧹 Очистить базу
PowerShell
Invoke-WebRequest -Uri "http://localhost:8080/api/contacts" -Method DELETE